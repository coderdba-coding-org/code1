apiVersion: v1
clusters:
- cluster:
    server: http://127.0.0.1:8080
  name: kubernetes
contexts:
- context:
    cluster: kubernetes
    user: system:node:{{ ansible_hostname }}
  name: system:node:{{ ansible_hostname }}@kubernetes
current-context: system:node:{{ ansible_hostname }}@kubernetes
kind: Config
preferences: {}
users:
- name: system:node:{{ ansible_hostname }}
